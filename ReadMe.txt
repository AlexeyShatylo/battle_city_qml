Created by Oleksii Shatylo as test task
Windows: all works fine (tested)
Linux (Ubuntu 16.04) - if media don't works(error : "defaultServiceProvider::requestService(): no service found for - "org.qt-project.qt.mediaplayer" ") use : sudo apt-get install gstreamer0.10* 
This will fix output streaming of media. Tested.
MacOS - not tested () - have no device to test app. 
